import UnifiedExplorer from "@/components/area/UnifiedExplorer";

export const metadata = {
  title: "Shibuya — Unified station & lightweight city",
  description:
    "Compare the registered station drawing with compact PLATEAU building geometry and a geographic map.",
};

export default function AreaPage() {
  return <UnifiedExplorer />;
}
