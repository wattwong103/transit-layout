import AreaExplorer from "@/components/area/AreaExplorer";

export const metadata = { title: "Shibuya — Full PLATEAU source viewer" };

export default function FullAreaPage() {
  return <AreaExplorer />;
}
